interface BackgroundProps {
	temperature: number,
	weatherCode: number,
	isDay: boolean
}
export default function getBackground ({
temperature, weatherCode, isDay
}: BackgroundProps): string {
	
  return '';

}